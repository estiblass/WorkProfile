apiVersion: v1
kind: Service
metadata:
  name: workprofile
spec:
  type: NodePort
  selector:
    app: workprofile
  ports:
    - protocol: TCP
      port: 5000        # פורט פנימי בשירות
      targetPort: 5000  # הפורט באפליקציה (בפוד)
      nodePort: 30080   # פורט חיצוני קבוע לגישה מה-Node
